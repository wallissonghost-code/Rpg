import {MapSystem} from "./mapSystem.js";
export function createCoordinates(){const el=document.querySelector("#coordinates");let elapsed=Infinity,lastX=null,lastY=null;return{update(dt){elapsed+=dt;if(elapsed<.1)return;elapsed=0;const p=MapSystem.playerPosition(),x=Math.round(p.x),y=Math.round(p.y);if(x===lastX&&y===lastY)return;lastX=x;lastY=y;el.textContent=`X: ${String(x).padStart(3,"0")}  Y: ${String(y).padStart(3,"0")}`}}}
